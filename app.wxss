@import "./styles/iconfont.wxss";

page,
view,
navigator,
text,
swiper,
swiper-item,
rich-text,
button,
input,
image {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

image {
  width: 100%;
}

page {
  /* 定义主题颜色 */
  /* 14px */
  font-size: 24rpx;
  width: 750rpx;
  color: #000;
  background-color: #f5f5f5;
}